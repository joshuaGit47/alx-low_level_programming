#include "main.h"
/**
 * _isdigit - functtion checks for digit (0 through 9)
 * %c: inter to be checked
 * Return: 1 if c iis a digit, 0 otherwise
 */
int _isdigit(int c)
{
return (c >= '0' && c <= '9');
}
